stages:
  - test

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - when: always

# Simple placeholder job
job_success:
  stage: test
  script:
    - echo "✅ Pipeline ran successfully."
  tags:
    - vcv2-dedicated-runner